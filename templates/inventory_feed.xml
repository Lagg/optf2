$def with (user, pack, items)
<?xml version="1.0" encoding="UTF-8"?>

<rss version="2.0">
  <channel>
    <title>$user.get_persona()'s TF2 Backpack</title>
    <generator>$project_name</generator>
    <link>${instance.homedomain}${virtual_root}</link>
    <image>
      <link>${instance.homedomain}${virtual_root}</link>
      <url>${instance.homedomain}${static_prefix}optf2_icon.png</url>
      <title>$project_name</title>
    </image>
  $items.reverse()
  $for item in items:
      <item>
        $ permalink = "{0}{1}item/{2}".format(instance.homedomain, virtual_root, pack.get_item_id(item))
        $ level = item["optf2_level"]
        $ type = item["optf2_type"]
        $ name = pack.get_item_custom_name(item)
        $ image = item['optf2_image_url']
        $if not name:
            $ name = pack.get_item_name(item)
        <description>Level $level $type</description>
        <title>$:item["optf2_feed_name"]</title>
        <link>$permalink</link>
        <guid isPermaLink="true">$permalink</guid>
        <enclosure type="image/png" length="0" url="$image"/>
      </item>
  </channel>
</rss>
